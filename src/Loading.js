import React from 'react';

const Loading = () => {
  return (
    <div className="loading">
      
      <h1>loading...</h1>
      {/* we can also have any animation here if we want it before loading */}
    </div>
  );
};

export default Loading;
